from enum import Enum

class ProductUnit(Enum):
    KG = "kg"
    PIECE = "piece"
    PACK = "pack"
    LITRE = "litre"
    BOTTLE = "bottle"
    DOZEN = "dozen"
    CRATE = "crate"
    BAG = "bag"
    BUNCH = "bunch"
    # Add others as needed
