# Policy module

Runtime intent policy is loaded from **`../config/intent_policy.yaml`** (see `intent_policy._load_unified_policy_impl`).

The former `intent_policy.yaml` in this directory was an obsolete draft and has been removed.
