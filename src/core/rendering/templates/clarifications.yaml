MISSING_TIME:
  template: "What time would you like the {{service}} appointment?"
  required_fields:
    - service

MISSING_TIME_ATTEMPT_2:
  template: "I still need to know what time you'd like for the {{service}} appointment."
  required_fields:
    - service

MISSING_DATE:
  template: "What date would you like for the {{service}}?"
  required_fields:
    - service

MISSING_DATE_ATTEMPT_2:
  template: "I still need to know what date you'd like for the {{service}}."
  required_fields:
    - service

NEEDS_CLARIFICATION:
  template: "I need a bit more information to help you."
  required_fields: []

