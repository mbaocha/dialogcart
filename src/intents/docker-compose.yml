services:
  intent_classifier:
    image: dialogcart-intent-classifier   # 👈 use the image built in run.sh
    container_name: dialogcart-intent-classifier
    environment:
      - INTENT_CLASSIFIER_PORT=9000
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PYTHONPATH=/app/src
      - OMP_NUM_THREADS=2
      - MKL_NUM_THREADS=2
      - TF_CPP_MIN_LOG_LEVEL=2
      - RASA_TELEMETRY_ENABLED=false
      - SQLALCHEMY_SILENCE_UBER_WARNING=1
    ports:
      - "9000:9000"
    volumes:
      - ./trainings/storage:/app/storage
      - ./:/app/src/intents
    deploy:
      resources:
        limits:
          memory: 8g
        reservations:
          memory: 2g
